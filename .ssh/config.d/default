# location:    $HOME/.ssh/config.d/default or similar
# in use with: $HOME/.ssh/config.d/local or similar
# with alias:  alias ssh="cat $HOME/.ssh/config.d/* > $HOME/.ssh/config; ssh"

Host *
    ServerAliveInterval 60
    GSSAPIAuthentication no

# vim: ft=sshconfig

